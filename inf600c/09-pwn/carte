flag 1 : 
on veut overwrite sur le buffer et utiliser la vulenarabilité sur gets pour appeler chemin sur l'adresse de retour.
donc pour username 24 A, pour 32 X pour la response et 8 X pour le pointer de username et apres on met l'adresse de chemin qui montre le flag 1.
payload : printf "XXXXXXXXXXXXXXXXXXXXXXXXAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\xf6\x92\x04\x8" | ./carte

flag 2: 
payload 'XXXXXXXXXXXXXXXXXXXXXXXAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\x00\xc0\x04\x08AAAA\x6d\x93\x04\x08'

flag 3:
on veut faire lancer un shell pour affiche le flag secret.
On veut donc faire évaluer à system non pas le contenu entre parenthèse mais la variable volatile secret qui contient '/bin/sh'.
on trouve son adresse avec info var dans gdb '0x0804c048  secret'.
0x1fea +0x0804c048:  on veut mettre dans eax (voir le code en objdump) pour que system call le eax.
on fait ce clacul et on le met dans le payload d'avant .
on a donc lancé un shell et on peut afficher tous les flags.
